import { MapCinema } from "@bsab/api/map/map";

export const DEFAULT_MAP_CINEMA: MapCinema = {
   videoID: '',
   title: '',
   author: 'Auto',
   duration: 0,
   offset: 0,
   bundledConfig: false,
   bloom: 1,
}
